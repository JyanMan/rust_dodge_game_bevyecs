use crate::core::renderer::*;
use crate::ecs::system::*;
use crate::ecs::ecs::*;
use crate::resources::area_manager::*;

// pub fn area_manager_init(ecs: &mut ECS, _renderer: &mut Renderer) {
//     ecs.add_resource::<AreaManager>(AreaManager::new());
// }
